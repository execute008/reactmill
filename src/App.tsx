import React from 'react';
import { Game } from './';

const App: React.FC = () => {
  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <Game />
    </div>
  );
};

export default App;
